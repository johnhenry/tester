name: Test

on:
  push:
  pull_request:

jobs:
  test:
    uses: johnhenry/workflows/.github/workflows/ci.yml@v1
    with:
      node-versions: '["20", "22", "24"]'
      os: '["ubuntu-latest"]'
      gate-commands: |
        npm test
        npm run examples
